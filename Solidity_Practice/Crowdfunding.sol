pragma solidity ^0.4.18;

contract Crowdfunding {
  /* address ideaCreator;
  uint256 balance = 0;
  // Set Crowdfunding function
  function setFunding() payable public {
    uint goalValue = 100;
    uint goalDate = 7;

    if(goalValue >=100 && goalDate <=7){
      ideaCreator.transfer(msg.value);
    } else {
      msg.sender.transfer(msg.value);
    }
  }

  // Get ideaCreator's address
  function set(address _ideaCreator) public {
      ideaCreator = _ideaCreator;
  }

  // Send ether
  function sending() payable public {
    balance = msg.value;
    Sended(msg.value, balance);
  }

  // Loging event
  event Sended{
    uint256 _value;
    uint256 _balance;
  }; */
}
